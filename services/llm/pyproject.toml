[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "chalybcrypto-llm"
version = "0.0.1"
description = "Async Claude analyst for ChalyCrypto. NEVER in the execution path."
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27,<1",
  "pydantic>=2.7,<3",
  "chalybcrypto-shared",
]

[tool.setuptools.packages.find]
where = ["src"]
